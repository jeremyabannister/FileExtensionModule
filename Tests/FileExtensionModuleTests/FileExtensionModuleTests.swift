import XCTest
@testable import FileExtensionModule

final class FileExtensionModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FileExtensionModule().text, "Hello, World!")
    }
}
